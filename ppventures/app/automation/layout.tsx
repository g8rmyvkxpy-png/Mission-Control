import type { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'AI Automation Suite | Start Free Trial',
  description: 'AI agents that find leads, write outreach, and follow up automatically. 14-day free trial. No credit card required.',
};

export default function AutomationLayout({ children }: { children: React.ReactNode }) {
  return children;
}
